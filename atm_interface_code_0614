class atm:
    verify = False
    num = 0
    def __init__(self,name):
        name = self.name

    def askname(self):
        name = input("please enter your name")
        print(name)
    def askaccno(self):
        accno = input("please enter you account no:")
        print(accno)
    def verifyauth(self):
        if (name in list[users.full_name]):
            if (accno in list[accounts.account_no])
                verified = True;
    def askusertopick(self):
        num = input("please select 1 for balance.\nPlease select 2 for Withdrawal.\nPlease select 3 for deoposit")
    def action(self):
        if(num ===1):
            print("the current account balance is "+account_details.balance)
        if(num===2):
            if(userwithdrawalamt <= balance):
                print("please take the money and card")
            else:
                print("sorry exceeds balance")
        else:
            num=input("please enter the amount to deposit")
            balance = balance + num
            print(balance)
    
